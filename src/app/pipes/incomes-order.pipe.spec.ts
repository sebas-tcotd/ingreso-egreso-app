import { IncomesOrderPipe } from './incomes-order.pipe';

describe('IncomesOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new IncomesOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
